# Core dependencies
funasr>=1.1.0
modelscope>=1.10.0
sounddevice>=0.4.6
numpy>=1.24.0

# PyTorch — for GPU/CUDA acceleration install the CUDA build manually:
#   pip install torch torchaudio --index-url https://download.pytorch.org/whl/cu121
# For CPU-only:
#   pip install torch torchaudio
torch>=2.0.0
torchaudio>=2.0.0
